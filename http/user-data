#cloud-config
autoinstall:
    version: 1
    early-commands:
        # workaround to stop ssh for packer as it thinks it timed out
        - sudo systemctl stop ssh
    locale: en_US
    keyboard:
        layout: en
        variant: us
    packages: [open-vm-tools, openssh-server, cloud-init]
    identity:
        hostname: ubuntu-server
        username: ubuntu
        password: "$6$rounds=4096$DuGhg7PIdXJP6FPg$T8z0pBPgZw2HzyxdKOvbqcLjI2EFyPtssi3H6b1ocL8V7xv40LfkCT.cOixIKDo8OidBCWklzuWvJFTjG6CSP1"
    ssh:
        install-server: true
        allow-pw: true
    storage:
        layout:
            name: direct
    user-data:
        disable_root: false
    late-commands:
        - echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/ubuntu
        - curtin in-target --target=/target -- chmod 440 /etc/sudoers.d/ubuntu
